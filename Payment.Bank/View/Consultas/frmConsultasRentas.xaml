<Controls:MetroWindow TitleCaps="False" x:Class="Payment.Bank.View.Consultas.frmConsultasRentas" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro" Title="titleQueriesRental" ShowTitleBar="True" TitlebarHeight="60" ShowIconOnTitleBar="True" TitleAlignment="Left" Height="600" Width="800" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" BorderThickness="1" ShowCloseButton="False" WindowTitleBrush="{StaticResource BackgroundHeader}" TitleForeground="White" GlowBrush="{DynamicResource BackgroundHeader}" ShowInTaskbar="False">
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="98*" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="70*" />
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>
        <Grid Name="gridMenu" DataContext="{Binding}" Grid.Row="0" Width="Auto" Background="Transparent" Grid.Column="1" Margin="0,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Name="gridDatosPersonales">

                <TextBlock  Margin="230,25,0,0" Name="lblDocumento" TextWrapping="Wrap" Text="{Binding Path=Personas.Documento}" VerticalAlignment="Top" HorizontalAlignment="Left" Width="201" Height="25"/>
                <TextBlock  Margin="230,49,0,0" Name="lblNombres" TextWrapping="Wrap" Text="{Binding Path=Personas.Nombres}" VerticalAlignment="Top" HorizontalAlignment="Left" Width="201" Height="25"/>
                <TextBlock  Margin="230,75,0,0" Name="lblApellidos" TextWrapping="Wrap" Text="{Binding Path=Personas.Apellidos}" VerticalAlignment="Top" HorizontalAlignment="Left" Width="201" Height="25"/>
                <TextBlock  Margin="230,99,0,0" Name="lblCiudad" TextWrapping="Wrap" Text="{Binding Path=Personas.Ciudades.Ciudad}" VerticalAlignment="Top" HorizontalAlignment="Left" Width="201" Height="25"/>
                <TextBlock  Margin="0,25,16,0" Name="lblContrato" FontWeight="Bold" TextWrapping="Wrap" Text="{Binding Path=AlquilerID}" VerticalAlignment="Top" HorizontalAlignment="Right" TextAlignment="Right" Width="108" Height="25"/>
                <TextBlock  Margin="0,52,16,0" Name="lblFecha" TextWrapping="Wrap" Text="{Binding Path=Fecha, StringFormat=dd/MM/yyyy}" VerticalAlignment="Top" HorizontalAlignment="Right" TextAlignment="Right" Width="108" Height="25"/>
                <TextBlock  Margin="0,76,16,0" Name="lblMonto" TextWrapping="Wrap" Text="{Binding Path=Monto, StringFormat=N2}" VerticalAlignment="Top" HorizontalAlignment="Right" TextAlignment="Right" Width="108" Height="25"/>

                <Border Background="Transparent" BorderBrush="black" BorderThickness="1" CornerRadius="5,5,5,5" HorizontalAlignment="Left" Margin="10,11,0,0" x:Name="BorderPhotoSrc" Width="120" Height=" 120" VerticalAlignment="Top">
                    <Grid x:Name="gridPhotoSrc">
                        <Image DataContext="{Binding}" Margin="2,2,2,2" x:Name="photoUsuario" Source="{Binding Path=Solicitudes.Clientes.Personas.Fotografias.Foto}">
                            <Image.Effect>
                                <DropShadowEffect BlurRadius="10" Color="#FF5D7183" Opacity="0.5" ShadowDepth="0" />
                            </Image.Effect>
                        </Image>
                    </Grid>
                </Border>

            </Grid>


            <TextBlock Text="lblLastName" HorizontalAlignment="Left" Margin="135,74,0,0" TextAlignment="Right" TextWrapping="Wrap" VerticalAlignment="Top" Width="80"/>
            <TextBlock Text="lblNames" HorizontalAlignment="Left" Margin="135,49,0,0" TextAlignment="Right" TextWrapping="Wrap"  VerticalAlignment="Top" Width="80"/>
            <TextBlock Text="lblDocument" HorizontalAlignment="Left" Margin="135,24,0,0" TextAlignment="Right" TextWrapping="Wrap" VerticalAlignment="Top" Width="80"/>
            <TextBlock Text="lblCiudad" HorizontalAlignment="Left" TextAlignment="Right" Margin="135,99,0,0" TextWrapping="Wrap"  VerticalAlignment="Top" Width="80"/>
            <TextBlock HorizontalAlignment="Right" Margin="0,50,112,0" TextWrapping="Wrap" TextAlignment="Right" Text="lblDateOfContract" VerticalAlignment="Top" Width="158"/>
            <TextBlock HorizontalAlignment="Right" Margin="0,75,112,0" TextWrapping="Wrap" TextAlignment="Right" Text="lblMonto" VerticalAlignment="Top" Width="112"/>
            <TextBlock HorizontalAlignment="Right" Margin="0,24,112,0" TextWrapping="Wrap" TextAlignment="Right" Text="lblContract" VerticalAlignment="Top" Width="153"/>

            <DataGrid AutoGenerateColumns="False" Margin="0,10,0,10" Name="dataGrid1" Grid.Row="1" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeColumns="False" CanUserResizeRows="False" HeadersVisibility="Column" HorizontalScrollBarVisibility="Hidden" >
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="colNumber" Width="90" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Path=RentaID, StringFormat=000}" Margin="0,0,5,0" TextAlignment="Center" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="colDate" x:Name="colDate" Width="90" Binding="{Binding Path=Fecha, StringFormat=dd/MM/yyyy}" IsReadOnly="True" />
                    <DataGridTextColumn Header="colConcept" x:Name="colConcept" Width="190*" Binding="{Binding Path=Concepto, StringFormat=dd/MM/yyyy}" IsReadOnly="True" />

                    <DataGridTemplateColumn Header="colAmount" Width="120" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Path=Monto,StringFormat=N2}" Margin="0,0,5,0" TextAlignment="Right" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0" Grid.Column="1" VerticalAlignment="Top" Height="55" Grid.Row="2">
            <TextBox Name="txtMonto" Visibility="Collapsed" HorizontalAlignment="Right" Height="26" Margin="0,16,10,0" Grid.Row="1" Text="{Binding Monto, StringFormat=N2, Mode=TwoWay, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" TextWrapping="Wrap" TabIndex="0" TextAlignment="Right" Width="90"  Grid.Column="1" VerticalAlignment="Top"/>
            <Button Height="55"  Visibility="Collapsed" Margin="0" Name="btnAceptar" Width="55" IsDefault="True">
                <Image Width="40" Height="40" Source="/Images/check.png" />
            </Button>
            <Button Height="50"  Visibility="Collapsed" Margin="0" Name="btnNotaDebito" Width="50" IsDefault="True">
                <Image Width="35" Height="35" Source="/Images/NotaDebito.png" />
            </Button>
            <Button Height="50"  Visibility="Collapsed" Margin="0" Name="btnNotaCredito" Width="50" IsDefault="True">
                <Image Width="35" Height="35" Source="/Images/NotaCredito.png" />
            </Button>
            <Button Height="55"   Margin="0" Name="btnPrint" Width="55" IsCancel="False" Visibility="Collapsed">
                <Image Width="40" Height="40" Source="/Images/Cuotas.png" />
            </Button>
            <Button Height="55"   Margin="0" Name="btnSalir" Width="55" IsCancel="False">
                <Image Width="40" Height="40" Source="/Images/back.png" />
            </Button>
        </StackPanel>
        <TextBlock Foreground="Red" FontWeight="Bold" VerticalAlignment="Center" Name="lblAtraso" Text="lblAtraso" Grid.Row="1" HorizontalAlignment="Left" Width="170" Margin="10,19,0,19" Height="32" Grid.Column="1"/>

    </Grid>
</Controls:MetroWindow>
