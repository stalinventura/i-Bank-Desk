<Controls:MetroWindow TitleCaps="False" x:Class="Payment.Bank.View.Formularios.frmPermisos" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:Payment.Bank.Helpers"  xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro" Title="titleRoles" 
    ShowTitleBar="True" TitlebarHeight="60" ShowIconOnTitleBar="True" TitleAlignment="Left" Height="600" Width="800" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" BorderThickness="1" ShowCloseButton="False" WindowTitleBrush="{StaticResource BackgroundHeader}" TitleForeground="White" GlowBrush="{DynamicResource BackgroundHeader}" ShowInTaskbar="False">
      <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.Resources>
            <local:EtiquetasConverter x:Key="EtiquetasConverter"/>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="26*" />
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="70*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="70*" />
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" Grid.Column="1" Name="gridPermisos" DataContext="{Binding}">
            <DataGrid AutoGenerateColumns="False" BorderBrush="{StaticResource BackgroundHeader}" BorderThickness="1" Margin="0" Name="dataGrid1" Grid.Row="1" HeadersVisibility="Column" HorizontalScrollBarVisibility="Hidden" CanUserAddRows="False" CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="colPermission" Width="*" Binding="{Binding Path=Permiso, Converter={StaticResource EtiquetasConverter}}" IsReadOnly="True" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <TextBlock Foreground="{StaticResource BackgroundHeader}" FontWeight="Bold" x:Name="lblTotal" Text="TOTAL:" Grid.Row="3" Grid.Column="1" HorizontalAlignment="Left" Width="173" Margin="10,0,0,39" Height="21" VerticalAlignment="Bottom"/>

        <Grid Grid.Row="1" Grid.Column="2" HorizontalAlignment="Center" Name="gridBotones" DataContext="{Binding}">
            <Grid.RowDefinitions>
                <RowDefinition Height="80*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="80*"/>
            </Grid.RowDefinitions>
            <Button x:Name="btnAdd" Grid.Row="1" HorizontalAlignment="Left" Width="40" Height="40">
                <Image Width="30" Height="30" Source="/Images/next.png" />
            </Button>
            <Button x:Name="btnDelete" Grid.Row="2" HorizontalAlignment="Left" Width="40" Height="40">
                <Image Width="30" Height="30" Source="/Images/back.png" />
            </Button>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="3"  Name="gridPermisosRoles" DataContext="{Binding}">
            <DataGrid AutoGenerateColumns="False" BorderBrush="{StaticResource BackgroundHeader}" BorderThickness="1" Margin="0" Name="dataGrid2" HeadersVisibility="Column" HorizontalScrollBarVisibility="Hidden" CanUserAddRows="False" CanUserDeleteRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="colPermission" Width="*" Binding="{Binding Path=Permisos.Permiso, Converter={StaticResource EtiquetasConverter}}" IsReadOnly="True" />
                    <DataGridTemplateColumn Header="colAdd" Width="85" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Agregar}" Margin="30,0,0,0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="colEdit" Width="85" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Modificar}" Margin="30,0,0,0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="colDelete" Width="85" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Eliminar}" Margin="30,0,0,0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="colPrint" Width="85" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding Imprimir}" Margin="30,0,0,0" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        <TextBlock Foreground="{StaticResource BackgroundHeader}" FontWeight="Bold" x:Name="lblTotal1" Text="TOTAL:" Grid.Row="2" HorizontalAlignment="Left" Width="190" Margin="10,0,0,39" Height="21" VerticalAlignment="Bottom" Grid.Column="3" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,10,0" Grid.Column="3" VerticalAlignment="Top" Height="55" Grid.Row="2" Width="110">
            <Button Height="55" Margin="0" Name="btnAceptar" Width="55" IsDefault="True">
                <Image Width="40" Height="40" Source="/Images/check.png" />
            </Button>
            <Button Margin="0" Name="btnSalir" Width="55" IsCancel="False">
                <Image Width="40" Height="40" Source="/Images/back.png" />
            </Button>
        </StackPanel>
    </Grid>
</Controls:MetroWindow>
