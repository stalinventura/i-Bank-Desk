<Controls:MetroWindow TitleCaps="False" x:Class="Payment.Bank.View.Formularios.frmReferencias" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro" Title="titleContact" ShowTitleBar="True" TitlebarHeight="60" ShowIconOnTitleBar="True" TitleAlignment="Left" Height="450" Width="355" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" BorderThickness="1" ShowCloseButton="False" WindowTitleBrush="{StaticResource BackgroundHeader}" TitleForeground="White" GlowBrush="{DynamicResource BackgroundHeader}" ShowInTaskbar="False">
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="80*" />
            <ColumnDefinition Width="0" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="80" />
        </Grid.RowDefinitions>
        <Grid Grid.Column="1" Name="gridReferenciasPersonales" DataContext="{Binding}">
            <ComboBox x:Name="cmbTipoReferencias" SelectedValue="{Binding TipoReferenciaID, Mode=TwoWay, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" Margin="10,46,19,0" VerticalAlignment="Top" Height="25" TabIndex="11"/>
            <TextBox Height="25" Margin="10,107,20,0" Name="txtNombres" Text="{Binding Referencia, Mode=TwoWay, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" VerticalAlignment="Top" />
            <TextBox Height="50" Margin="11,163,19,0" Name="txtDireccion"  Text="{Binding Direccion, Mode=TwoWay, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" VerticalAlignment="Top" />
            <TextBox Height="25" HorizontalAlignment="Left" Margin="11,248,0,0" x:Name="txtTelefono" Text="{Binding Telefono, Mode=TwoWay, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" VerticalAlignment="Top" Width="174" KeyDown="txtTelefono_KeyDown" LostFocus="txtTelefono_LostFocus" />
            <TextBlock Height="25" HorizontalAlignment="Left" Margin="12,77,0,0"  Text="lblNames" VerticalAlignment="Top" Width="154" />
            <TextBlock Height="25" HorizontalAlignment="Left" Margin="11,138,0,0" Text="lblAddress" VerticalAlignment="Top" Width="155" />
            <TextBlock Height="25" HorizontalAlignment="Left" Margin="11,223,0,0"  Text="lblPhone" VerticalAlignment="Top" Width="155" />            
            <TextBlock Text="lblReferenceType" HorizontalAlignment="Left" Margin="11,25,0,0" TextWrapping="Wrap"  VerticalAlignment="Top" Width="169"/>
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,10,0" Grid.Column="1" VerticalAlignment="Top" Height="55" Grid.Row="1">
            <Button Height="55" Margin="0" Name="btnAceptar" Width="55" IsDefault="True">
                <Image Width="40" Height="40" Source="/Images/check.png" />
            </Button>
            <Button Margin="0" Name="btnSalir" Width="55" IsCancel="False">
                <Image Width="40" Height="40" Source="/Images/back.png" />
            </Button>
        </StackPanel>
    </Grid>
</Controls:MetroWindow>
