<Window x:Class="Payment.Bank.frmPrincipalView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vc="clr-namespace:Visifire.Charts;assembly=WPFVisifire.Charts"
        xmlns:local="clr-namespace:Payment.Bank"
        xmlns:helper="clr-namespace:Payment.Bank.Helpers"
        xmlns:img="clr-namespace:Payment.Bank.Helpers" xmlns:notifications="clr-namespace:Notifications.Wpf.Controls;assembly=Notifications.Wpf"
        mc:Ignorable="d"
        Title="Software Architec S.R.L" WindowState="Normal" Width="{Binding Source={x:Static SystemParameters.PrimaryScreenWidth}}" Height="{Binding Source={x:Static SystemParameters.PrimaryScreenHeight}}" WindowStartupLocation="CenterScreen" AllowsTransparency="True" BorderThickness="1" BorderBrush="{StaticResource BackgroundHeader}" WindowStyle="None" Icon="/Images/Icono.ico">
    <Grid Name="Layout" Margin="0,2">
        <Grid.Resources>
            <img:ImageSourceConverter x:Key="ImageSourceConverter" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="80"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <DockPanel Background="Black" Grid.Row="0" VerticalAlignment="Center" Margin="-3">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock HorizontalAlignment="Left" Grid.Column="0" Margin="10,0" FontSize="22" TextWrapping="Wrap" Text="i-Bank" FontFamily="Segoe UI" FontWeight="Light" Foreground="White" VerticalAlignment="Top" Height="30" Width="255"/>

                <Button x:Name="btnMinimizar" Grid.Column="1" HorizontalAlignment="Right" Style="{StaticResource btnClose}" Margin="0" VerticalAlignment="Top" Width="30" Height="30">
                    <Image Width="30" Height="30" Source="Images/minus.png" />
                </Button>

                <Button x:Name="btnSalir" Grid.Column="2" HorizontalAlignment="Right" Style="{StaticResource btnClose}" Margin="0,0,5,0" VerticalAlignment="Top" Width="30" Height="30">
                    <Image Width="30" Height="30" Source="Images/cancel.png" />
                </Button>
            </Grid>
        </DockPanel>
        <DockPanel Background="{StaticResource BackgroundHeader}" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="100*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="lblWelcome" Grid.Column="0" HorizontalAlignment="Left" Margin="20,18,0,17" TextWrapping="Wrap" Visibility="Visible" VerticalAlignment="Center"  Height="46" Width="647" Foreground="White" FontWeight="Bold" FontSize="20"/>

                <TextBlock Foreground="White" Margin="0,5,5,0" Grid.Column="1" Name="lblNombres" TextWrapping="Wrap" Text="{Binding Path=Personas.Nombres}" FontWeight="Light" FontSize="24" VerticalAlignment="Top" HorizontalAlignment="Right" TextAlignment="Right" Width="290" Height="32" />
                <TextBlock Foreground="White" Margin="0,35,5,0" Grid.Column="1" TextWrapping="Wrap" Text="{Binding Path=Personas.Apellidos}" FontWeight="Light" FontSize="16" FontFamily="Segoe UI" VerticalAlignment="Top" HorizontalAlignment="Right" TextAlignment="Right" Width="290" Height="21" />
                <TextBlock Foreground="White" Margin="0,55,5,0" Grid.Column="1" FontWeight="Light" TextWrapping="Wrap" Text="{Binding Path=Roles.Rol}" FontSize="12" FontFamily="Segoe UI" VerticalAlignment="Top" HorizontalAlignment="Right" TextAlignment="Right" Width="290" Height="21" />
                <Border Background="Transparent" x:Name="border" Grid.Column="2" DataContext="{Binding}" BorderBrush="Transparent" BorderThickness="1" CornerRadius="2" Margin="0,2,10,0" Width="60" Height="70" HorizontalAlignment="Right" VerticalAlignment="Top" Grid.Row="1">
                    <Grid>
                        <Image Margin="2" x:Name="srcPhoto" Stretch="Fill" Source="{Binding Path=Personas.Fotografias.Foto, Mode=TwoWay, Converter={StaticResource ImageSourceConverter}}"/>
                    </Grid>
                </Border>

            </Grid>
        </DockPanel>
        <Grid Margin="10,0,0,0" Grid.Row="2" Name="gridMenu" Height="35"/>
        <Grid Margin="0,0" Grid.Row="3" Visibility="Visible">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30*" />
                <ColumnDefinition Width="40*" />
                <ColumnDefinition Width="30*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="200*"/>
                <RowDefinition Height="500"/>
                <RowDefinition Height="200*"/>
            </Grid.RowDefinitions>
            <Image Name="imgLogo" Visibility="Visible" Margin="80,80" Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Source="Images/logo/logo.png"/>
            <TextBlock x:Name="lblPlan" Visibility="Hidden" FontWeight="Bold" FontFamily="Segoe UI"  Foreground="Black" HorizontalAlignment="Right" VerticalAlignment="Bottom" TextWrapping="Wrap" Text="{Binding Sucursales.Empresas.Planes.Plan, StringFormat='Plan {0}'}" Width="250" Margin="0,0,10,0" Height="30" Grid.Column="2" Grid.Row="2" TextAlignment="Right"/>
            <notifications:NotificationArea x:Name="WindowArea" Position="BottomRight" Margin="0,0,5,5" MaxItems="5" Grid.Column="2" Grid.RowSpan="2" Grid.Row="1"/>
            <Image x:Name="logo" HorizontalAlignment="Left" Height="168" Margin="174,184,0,0" Grid.Row="1" VerticalAlignment="Top" Width="194"/>
        </Grid>
        <Grid Grid.Row="4" Background="Black" Margin="-2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="25*"/>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="25*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="txtTime" FontWeight="Light" FontFamily="Segoe UI" Foreground="White" FontSize="24" HorizontalAlignment="Left" TextWrapping="Wrap" Text="" VerticalAlignment="Center" Width="271" Margin="10,0,0,0" Grid.Column="0" Height="32"/>
            <Label x:Name="lblMensaje" FontWeight="Light" FontFamily="Segoe UI" Foreground="White" FontSize="14" VerticalAlignment="Center" HorizontalContentAlignment="Center" Margin="5,0" Grid.Column="1" Height="30"/>
            <StackPanel Visibility="Visible" Margin="10,0" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Grid.Row="5" Background="Transparent">
                <Grid Width="200">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="30"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="lblVersion" FontWeight="Bold" Grid.Column="0" VerticalAlignment="Center" FontFamily="Segoe UI" Foreground="White" HorizontalAlignment="Right" TextWrapping="Wrap" Text="Version 1.0.0" TextAlignment="Right"/>

                    <Button HorizontalAlignment="Center" Grid.Column="1" x:Name="btnAbout" Visibility="Visible" Margin="0,0,0,0" VerticalAlignment="Top" Width="35" Height="35" Grid.Row="5">
                        <Image Width="35" Height="35" Source="Images/Settings.png" />
                    </Button>

                    <Button HorizontalAlignment="Center" Grid.Column="2" x:Name="btnPassword" Visibility="Collapsed" Margin="0,0,0,0" VerticalAlignment="Top" Width="35" Height="35" Grid.Row="5">
                        <Image   Width="20" Height="20" Source="Images/appbar_password.png" />
                    </Button>
                </Grid>
            </StackPanel>
        </Grid>

    </Grid>
</Window>